import { PlaceholderScreen } from '../components';

export function SettingsScreen() {
  return (
    <PlaceholderScreen
      title="Settings"
      hint="Active hours, goals, sound, and theme will be adjustable here (slice 02+)."
    />
  );
}
